import { useRouter } from "next/router"
export default function MenuList() {
    const router = useRouter()
    const menuList = []
    return <>
        <div>
            메뉴 입니다.
        </div>
    </>
}